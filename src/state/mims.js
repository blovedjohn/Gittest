function Meme(){
   
   const [mem, setMem]=React.useState({
   topText: "",
   bottomText: "",
   randomImage: "http://i.img.png"
})
    
const [allmemeimage, setallmemeimage]= React.useState(MemData)
}
function Getmemimage(){
    const memarray= MemData.data.mem
    const randomnumber=math.floor(math.random()*memarray.lenght)
    const url=memarray[randomnumber].url
    function dataform(props){
    
}}

return(

    )
